function Info ({ title, location }) {
    return (
      <div className="info-logement">
        <h1 className="titre-logement">{title}</h1>
        <h1 className="loc-logement">{location}</h1>
      </div>
    );
  }
  
  export default Info;
  